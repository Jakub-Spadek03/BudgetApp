spring.application.name=PASIR_Spadek_Jakub
spring.config.import=optional:file:.env[.properties]
# MySQL Configuration
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/${MYSQL_DATABASE:spadek_jakub_k03_spring}?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
ASE:spadek_jakub_k03_spring}?allowPublicKeyRetrieval=true&us
eSSL=false&serverTimezone=UTC
spring.datasource.username=${MYSQL_USER:user-js}
spring.datasource.password=${MYSQL_PASSWORD:strongUserPass123!}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
spring.graphql.http.path=/graphql
spring.graphql.schema.file-extensions=.graphql,.graphqls,.gqls
app.jwt.secret=${JWT_SECRET}
